services:
  web:
    image: tayyabtahir/assetmanager:latest
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=${DATABASE_URL:-sqlite:////data/inventory.db}
      - SECRET_KEY=change-me
      - TZ=Australia/Brisbane
    volumes:
      - ./data:/data
